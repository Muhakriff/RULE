<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rule">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RULE">
        <activity android:name=".ChartActivity"></activity>
        <activity android:name=".KoqHistory" />
        <activity android:name=".KoqActivity" />
        <activity android:name=".PosterActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".HistoryRequest" />
        <activity android:name=".HistoryTask" />
        <activity android:name=".HistoryEvent" />
        <activity
            android:name=".EventActivity"
            android:label="@string/title_activity_event" />
        <activity
            android:name=".TaskActivity"
            android:label="@string/title_activity_main2" />
        <activity
            android:name=".RegisterUser"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>